{
  "name": "wulfy-browser",
  "version": "1.0.0",
  "description": "🌐 Wulfy Browser - Ein moderner, schneller Web-Browser mit Chromium-Engine",
  "author": "UEBlackWulfGHG",
  "license": "MIT",
  "main": "dist/main.js",
  "type": "commonjs",
  "scripts": {
    "dev": "concurrently \"npm run watch-ts\" \"npm run electron-dev\"",
    "watch-ts": "tsc --watch --outDir dist",
    "build-ts": "tsc --outDir dist && npm run copy-assets",
    "copy-assets": "cp src/index.html dist/ && cp src/styles.css dist/",
    "electron-dev": "npm run copy-assets && electron .",
    "start": "npm run build-ts && npm run electron-dev",
    "build": "npm run build-ts && electron-builder",
    "pack": "electron-builder --dir",
    "dist": "npm run build-ts && electron-builder",
    "prepare": "npm run build-ts"
  },
  "build": {
    "appId": "com.wulfy.browser",
    "productName": "Wulfy Browser",
    "files": [
      "dist/**/*",
      "node_modules/**/*"
    ],
    "directories": {
      "buildResources": "assets"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Utility"
    },
    "win": {
      "target": [
        "nsis",
        "portable"
      ]
    },
    "mac": {
      "target": [
        "dmg",
        "zip"
      ]
    }
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0",
    "electron-store": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "concurrently": "^9.0.0",
    "electron": "^42.2.0",
    "electron-builder": "^25.1.0",
    "typescript": "^6.0.3"
  }
}

